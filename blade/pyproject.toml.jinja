[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "{{ project_description }}"
readme = "README.md"
authors = [
    { name = "{{ author_name }}", email = "{{ author_email }}" }
]
requires-python = ">={{ python_version }}"
dependencies = []

[project.scripts]
train = "blade:train:train"
infer = "blade:infer:infer"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
